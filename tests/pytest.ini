[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -vs --tb=short -m "not integration"
markers =
    integration: 需要真实 GPU/模型加载的重型集成测试，日常开发不默认执行
